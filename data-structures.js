/**
 * Return a new array with items in reverse order
 * to those passed
 * @param {array} arr
 * @returns {array}
 */
function reverseArray (arr) {
  // ...
}

/**
 * Sum the items from a passed array
 * @param {array} arr
 * @returns {number}
 */
function sumArray (arr) {
  // ...
}

/**
 * Return a new array where each item in the passed
 * array is multiplied by 2
 * @param {array} arr
 * @returns {array}
 */
function multiplyArrayItemsBy2 (arr) {
  // ...
}

/**
 * Return a new array where only the even numbers
 * are included from the passed array items
 * Hint: look at the modulo function
 * @param {array} arr
 * @returns {array}
 */
function returnOnlyEvenNumbers (arr) {
  // ...
}

/**
 * Return a new array comprised of the strings
 * from the obj.data property
 * Hint: look at the 'typeof' operator
 * @param {object} obj
 * @returns {array}
 */
function extractStringsFromDataProp (obj) {
  // ...
}

module.exports = {
  reverseArray,
  sumArray,
  multiplyArrayItemsBy2,
  returnOnlyEvenNumbers,
  extractStringsFromDataProp
}